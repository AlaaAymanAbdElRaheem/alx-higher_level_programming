#!/usr/bin/python3
"""defining Base class"""



class Base:
    """implement Base class"""
    def __init__(self, id=None):
        """inistializing the class"""
        if id:
            Base.id = id
        else:
            Base.__nb_objects += 1
            self.id = Base.__nb_objects
