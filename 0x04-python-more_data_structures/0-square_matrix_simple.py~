#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    if len(matrix) > 0:
        new_matrix = []
        new_matrix = [[row[i] * row[i] for i in range(len(row))] for row in matrix]
        return new_matrix
